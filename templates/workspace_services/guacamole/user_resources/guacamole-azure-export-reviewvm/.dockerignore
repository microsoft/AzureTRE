# Local .terraform directories
**/.terraform/*

# TF backend files
**/*_backend.tf

# Porter files it uses for local build
./cnab/**

Dockerfile.tmpl

.env*

terraform/destroy.sh

# Shared files are provided by build contexts (windowsvm / import-reviewvm),
# not this one
terraform/vm
reset_password.sh
delete_vm_extensions.sh
terraform/main.tf
terraform/data.tf
terraform/locals.tf
terraform/variables.tf
terraform/outputs.tf
terraform/windowsvm.tf
terraform/download_review_data.ps1
