---
schemaVersion: 1.0.0
name: tre-shared-service-application-gateway
version: 0.1.25
description: "An Azure Application Gateway shared service"
dockerfile: Dockerfile.tmpl
registry: azuretre

credentials:
  - name: azure_tenant_id
    env: ARM_TENANT_ID
  - name: azure_subscription_id
    env: ARM_SUBSCRIPTION_ID
  - name: azure_client_id
    env: ARM_CLIENT_ID
  - name: azure_client_secret
    env: ARM_CLIENT_SECRET

parameters:
  - name: tre_id
    type: string
    description: "The ID of the parent TRE instance e.g., mytre-dev-3142"
  - name: id
    type: string
    description: "Resource ID"
  - name: tfstate_resource_group_name
    type: string
    description: "Resource group containing the Terraform state storage account"
  - name: tfstate_storage_account_name
    type: string
    description: "The name of the Terraform state storage account"
  - name: tfstate_container_name
    type: string
    default: "tfstate"
    description: "The name of the Terraform state storage container"
  - name: arm_use_msi
    env: ARM_USE_MSI
    type: boolean
    default: false
  - name: backend_collection
    type: string
    default: "W10=" # b64 for []
    description: "Backends list used to route traffic into specific TRE services."
  - name: arm_environment
    env: ARM_ENVIRONMENT
    type: string
    default: "public"
  - name: certificate_name
    env: CERTIFICATE_NAME
    type: string
  - name: ui_app_service
    env: UI_APP_SERVICE
    type: string


mixins:
  - terraform:
      clientVersion: 1.4.6
  - exec

install:
  # TODO: delete when we use Terraform 1.5+
  - exec:
      command: bash
      arguments:
        - ./import.sh
      flags:
        mgmt_resource_group_name: ${ bundle.parameters.tfstate_resource_group_name }
        mgmt_storage_account_name: ${ bundle.parameters.tfstate_storage_account_name }
        container_name: ${ bundle.parameters.tfstate_container_name }
        key: ${ bundle.parameters.tre_id }-${ bundle.name }_${ bundle.parameters.id }
        tre_id: ${ bundle.parameters.tre_id }
        azure_subscription_id: ${ bundle.credentials.azure_subscription_id }
        certificate_name: ${ bundle.parameters.certificate_name }
        ui_app_service: ${ bundle.parameters.ui_app_service }
      envs:
        ARM_USE_MSI: ${ bundle.parameters.arm_use_msi }
        ARM_ENVIRONMENT: ${ bundle.parameters.arm_environment }

  # TODO: uncomment when we use Terraform 1.5+
  # - terraform:
  #     description: "Install shared service"
  #     vars:
  #       tre_id: ${ bundle.parameters.tre_id }
  #       backend_collection_b64: ${ bundle.parameters.backend_collection }
  #     backendConfig:
  #       resource_group_name: ${ bundle.parameters.tfstate_resource_group_name }
  #       storage_account_name: ${ bundle.parameters.tfstate_storage_account_name }
  #       container_name: ${ bundle.parameters.tfstate_container_name }
  #       key: ${ bundle.parameters.tre_id }-${ bundle.name }_${ bundle.parameters.id }

upgrade:
  - terraform:
      description: "Upgrade shared service"
      vars:
        tre_id: ${ bundle.parameters.tre_id }
        backend_collection_b64: ${ bundle.parameters.backend_collection }
        certificate_name: ${ bundle.parameters.certificate_name }
        ui_app_service: ${ bundle.parameters.ui_app_service }
      backendConfig:
        resource_group_name: ${ bundle.parameters.tfstate_resource_group_name }
        storage_account_name: ${ bundle.parameters.tfstate_storage_account_name }
        container_name: ${ bundle.parameters.tfstate_container_name }
        key: ${ bundle.parameters.tre_id }-${ bundle.name }_${ bundle.parameters.id }

uninstall:
  - exec:
      description: "Tear down shared service"
      command: echo
      arguments:
        - "Dummy uninstall as the underlining Azure resource shouldn't be removed by this action."
